import { HomeView } from "@/sections/home/view";

export const metadata = {
  title: "AgSpeaks: The starting point for your next project",
};

export default function Home() {
  return <HomeView />;
}
